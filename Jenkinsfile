node("linux") {
  stage("TEST") {
    // echo "ENV: ${new groovy.json.JsonBuilder(env).toPrettyString()}"
    // echo "SCM: ${new groovy.json.JsonBuilder(scm).toPrettyString()}"
    // echo "current build: ${new groovy.json.JsonBuilder(currentBuild).toPrettyString()}"
  }
}
